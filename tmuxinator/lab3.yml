# /home/ned/.config/tmuxinator/lab3.yml

name: lab3
root: ~/Work/university/semester11/data-analysis/kpi-data-analysis-labs/lab3/

# Specifies (by index) which pane of the specified window will be selected on project startup. If not set, the first pane is used.
startup_window: editor
startup_pane: 1

pre_window: micromamba activate data-scince


windows:
    - editor:
        layout: main-vertical-mirror
        panes:
            - ipython
            - nvim
    - term:
