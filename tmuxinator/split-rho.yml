name: split-rho
root: ~/Work/imp/vasp/chains/split_rho/

# Specifies (by index) which pane of the specified window will be selected on project startup. If not set, the first pane is used.
startup_window: editor
startup_pane: 1

pre_window: micromamba activate data-scince


windows:
    - editor:
      - nvim +"Telescope fd"
    - term:
    - dash:
      - ./cohp_dos_plot.py
